// 🔄 자동 생성된 파일 – 수정하지 마세요!
// 이 파일은 scripts/generateQuestionFileMap.ts에 의해 생성됩니다.

import q0 from "@/assets/questions/복합발전운전(객관식,메타).json";
import q1 from "@/assets/questions/복합발전운전(주관식,메타).json";

import { QuestionFileMap } from "./types";

export const questionFileMap : QuestionFileMap = {
  "복합발전운전(객관식,메타).json": {
    name: q0.metadata?.title ?? "복합발전운전(객관식,메타)",
    data: q0,
  },
  "복합발전운전(주관식,메타).json": {
    name: q1.metadata?.title ?? "복합발전운전(주관식,메타)",
    data: q1,
  },
};
